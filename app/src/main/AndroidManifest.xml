<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pectolabs.todo">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToDoApp">
        <activity android:name="com.pectolabs.todo.ui.acitivities.AddTaskActivity"/>
        <activity android:name="com.pectolabs.todo.ui.acitivities.MainActivity" />
        <activity
            android:name="com.pectolabs.todo.ui.acitivities.LoginActivity"
            android:theme="@style/Theme.ToDoApp.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>